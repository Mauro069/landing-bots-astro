<section id="live-results" class="results">
  <h3 class="title">MY live results</h3>

  <span class="description">
    Here you will find my daily results that have been posted every day
    consecutively for more than a year, where we show profits and losses in a
    100% transparent way!
  </span>

  <a
    style="z-index: -1;"
    href="https://t.me/MYEASRESULTS"
    class="image-container"
  >
    <img class="tl-container" src="/assets/results.jpg" height="600" />
    <div class="tl">
      <img class="tl-img" width={75} height={75} src="/assets/telegram.webp" />
    </div>
  </a>
</section>

<style>
  @import "../globals.css";

  .results {
    color: white;
    display: flex;
    align-items: center;
    flex-direction: column;
    text-align: center;
    gap: 1rem;
    font-family: "N27";

    .title {
      font-family: "Good times";
      text-transform: uppercase;
      font-size: 32px;
    }

    .description {
      max-width: 450px;
      color: var(--color-2);
    }

    .image-container {
      width: 80vw;
      max-width: 330px;
      position: relative;
      transition-duration: 0.3s;
      cursor: pointer;
      border: 3px solid var(--color-3);
      box-shadow: 0px 0px 10px 2px var(--color-3);
      border-radius: 16px;

      .tl-container {
        width: 100%;
        position: relative;
        display: flex;
        border-radius: 16px;
        max-height: 600px;
        -o-object-fit: contain;
        object-fit: contain;
      }

      .tl {
        position: absolute;
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #00000075;
        top: 0;
        left: 0;
        border-radius: 16px;
      }

      .tl-img {
        width: 75px;
        height: 75px;
        box-shadow: 0px 0px 8px 2px var(--color-3);
        object-fit: contain;
        border-radius: 100%;
      }
    }

    .image-container:hover {
      transform: scale(1.02);
    }
  }

  @media (max-width: 450px) {
    .results {
      h3 {
        font-size: 24px;
      }
    }
  }
</style>
